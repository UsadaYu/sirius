list(APPEND TEST_LINK_LIBRARIES
     "${SIRIUS_TARGET_CMAKE_NAMESPACE}::${SIRIUS_KIT_LIBRARY_NAME}"
     "${SIRIUS_TARGET_CMAKE_NAMESPACE}::${SIRIUS_THREAD_LIBRARY_NAME}")

add_subdirectory(queue)
